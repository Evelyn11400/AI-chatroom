# === Local: copy this file to ".env.local" and fill values, then run: vercel dev ===
# === Vercel: Project → Settings → Environment Variables (do NOT commit real keys) ===

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
